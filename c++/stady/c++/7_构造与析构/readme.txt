构造函数 与 析构函数都属于类中默认存在的函数，不自行定义，编译器会送一个
另外：拷贝构造函数也是默认存在的（在类与类的赋值中调用：
                                eg:
                                    class A;
                                    class B = A；）
构造函数用于类成员声明初始化的自动调用：
    7.1~7.6
析构函数用于类成员清理时的自动调用：
    7.8